#pragma once

// Copy this file to secrets.h and fill in your own values.
// secrets.h is gitignored and must never be committed.

// Network credentials
#define WIFI_SSID "ssid"
#define WIFI_PASSWORD "password"

// Home Assistant, token from Profile -> Security -> Long-lived access tokens
#define HA_URL "https://homeassistant.example.com/api/services/script/turn_on"
#define HA_TOKEN "token"
